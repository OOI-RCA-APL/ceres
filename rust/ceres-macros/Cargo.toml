[package]
name = "ceres-macros"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Procedural macros for the Ceres Python bindings"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["extra-traits", "full"] }

[lints]
workspace = true
