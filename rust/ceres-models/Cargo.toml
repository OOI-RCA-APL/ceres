[package]
name = "ceres-models"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Generates the Python entity model modules from the native table schemas"

[build-dependencies]
ceres-database = { path = "../ceres-database" }
ceres-entities = { path = "../ceres-entities" }
serde_json = "1"

[lints]
workspace = true
