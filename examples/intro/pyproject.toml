[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "intro"
version = "0.1.0"
description = "APL - Ceres Intro"
authors = [{ name = "Jake Ploskey", email = "jploskey@uw.edu" }]
requires-python = ">=3.12"

# ceres = { path = "../..", develop = true }
