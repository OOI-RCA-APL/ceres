"""Engine operations behind the API's routes.

Each module holds the logic one route family needs, free of any transport concern. The
native server reaches them through the host's named operations, and the tests exercise
them directly.
"""
